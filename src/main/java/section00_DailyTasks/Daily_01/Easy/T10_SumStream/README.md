Реализуй метод `sumStream()`, который принимает список целых чисел `list` и возвращает сумму чисел, которые больше 10.

Примеры:

```
[1] // возвращает 0
[15] // возвращает 15
[-4, -3, 10, 15, 17] // возвращает 32
```